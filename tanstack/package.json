{
  "name": "tanstack",
  "description": "Module Federation Vite + TanStack is now possible",
  "scripts": {
    "dev": "pnpm --filter tanstack-host --filter tanstack-remote dev",
    "build": "pnpm --filter tanstack-host --filter tanstack-remote build",
    "preview": "pnpm --filter tanstack-host --filter tanstack-remote preview"
  }
}
