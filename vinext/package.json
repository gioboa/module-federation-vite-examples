{
  "name": "vinext",
  "description": "Module Federation Vite + vinext SSR with shared React and isolated islands",
  "scripts": {
    "dev": "pnpm --filter 'vinext-*' --parallel dev",
    "build": "pnpm --filter 'vinext-*' build",
    "preview": "pnpm build && pnpm --filter 'vinext-*' --parallel preview"
  }
}
